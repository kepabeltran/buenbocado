# ============================================================
# BuenBocado DB (Prisma) — packages/db/.env
# Copia este archivo como ".env" dentro de packages/db/
# (Para prisma migrate / studio desde el paquete db)
# ============================================================

DATABASE_URL=

# ============================================================
# BuenBocado Client (Customer + Admin) — apps/client/.env.local
# Copia este archivo como ".env.local" dentro de apps/client/
# ============================================================

# URL base de la API
# Dev: http://127.0.0.1:4000
# Prod: https://api.tudominio.com
NEXT_PUBLIC_API_URL=http://127.0.0.1:4000

# Alias opcional (algunos componentes miran esto)
NEXT_PUBLIC_API_BASE_URL=

# Redirección de /r/* hacia el portal restaurante
NEXT_PUBLIC_RESTAURANT_PORTAL_ORIGIN=http://127.0.0.1:3001

# Para enlaces directos (si lo usas en UI)
NEXT_PUBLIC_RESTAURANT_URL=http://127.0.0.1:3001

# Legacy / server-only override (normalmente NO hace falta)
BUENBOCADO_API_URL=
NEXT_PUBLIC_API_URL_BASE=

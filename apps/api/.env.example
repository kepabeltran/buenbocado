# ============================================================
# BuenBocado API — apps/api/.env
# Copia este archivo como ".env" dentro de apps/api/
# ============================================================

# Puerto de la API (dev)
PORT=4000

# Obligatorio (en prod la API aborta si falta)
JWT_SECRET=<REPLACE_ME>

# Base URL pública (para generar links absolutos)
# Dev: http://127.0.0.1:4000
# Prod: https://api.tudominio.com
PUBLIC_BASE_URL=http://127.0.0.1:4000

# CORS (lista exacta de origins)
# Dev típico (local + LAN):
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://192.168.1.134:3000,http://192.168.1.134:3001
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001

# Postgres (dev con Docker Compose)
# Ajusta a tu setup. Ej (dev):
# DATABASE_URL="postgresql://buenbocado:buenbocado@127.0.0.1:5432/buenbocado?schema=public"
DATABASE_URL=

# Endpoints internos sensibles (solo si lo habilitas en prod)
ENABLE_INTERNAL_ENDPOINTS=false
X_INTERNAL_KEY=<REPLACE_ME>

# Emails (opcional; actualmente Resend está comentado en código)
RESEND_API_KEY=
MAIL_FROM=BuenBocado <hola@buenbocado.es>

# URL del portal restaurante (para enlaces en emails)
APP_URL_RESTAURANT=http://127.0.0.1:3001

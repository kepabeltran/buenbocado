# ============================================================
# BuenBocado — .env (PROD / docker-compose.prod.yml)
# Copia este archivo como ".env" en el ROOT del repo.
#
# ⚠️  IMPORTANTE
# - En producción la API NO arranca si faltan:
#     - JWT_SECRET
#     - CORS_ORIGINS
#     - DATABASE_URL
# - Caddy usará estos dominios para TLS automático (Let's Encrypt).
# ============================================================

# ===== DOMINIOS (PROD) =====
API_DOMAIN=
APP_DOMAIN=
RESTAURANT_DOMAIN=

# Email para Let's Encrypt (Caddy)
ACME_EMAIL=

# ===== BASE URL PUBLICA DE LA API (PROD) =====
# Ej: https://api.tudominio.com
PUBLIC_BASE_URL=

# ===== NEXT (client + restaurant) =====
# Ej: https://api.tudominio.com
NEXT_PUBLIC_API_URL=

# Portal restaurante (para redirecciones /r/* desde el Client)
# Ej: https://r.tudominio.com
NEXT_PUBLIC_RESTAURANT_PORTAL_ORIGIN=
NEXT_PUBLIC_RESTAURANT_URL=

# ===== CORS (PROD) =====
# Coma-separated list of allowed origins (exact match), example:
# CORS_ORIGINS=https://app.tudominio.com,https://r.tudominio.com
CORS_ORIGINS=

# ===== JWT (OBLIGATORIO EN PROD) =====
# Genera un secreto largo (>= 32 chars). Ej (PowerShell):
#   [Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Max 256 }))
JWT_SECRET=

# ===== POSTGRES =====
POSTGRES_USER=buenbocado
POSTGRES_PASSWORD=CAMBIA_ESTA_PASSWORD
POSTGRES_DB=buenbocado

# ===== DATABASE_URL (PROD usa hostname "postgres") =====
# Ej:
# DATABASE_URL="postgresql://buenbocado:CAMBIA_ESTA_PASSWORD@postgres:5432/buenbocado?schema=public"
DATABASE_URL=

# ===== Opcionales (solo si activas endpoints internos / emails) =====
# Habilita endpoints internos sensibles SOLO si sabes lo que haces.
# ENABLE_INTERNAL_ENDPOINTS=true
ENABLE_INTERNAL_ENDPOINTS=

# Clave interna para endpoints “soporte” (dev / emergencias).
# X_INTERNAL_KEY=<REPLACE_ME>
X_INTERNAL_KEY=

# Emails (ahora mismo Resend está comentado en código; esto es futuro-ready)
RESEND_API_KEY=
MAIL_FROM=BuenBocado <hola@buenbocado.es>
APP_URL_RESTAURANT=
